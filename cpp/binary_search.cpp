#include <bits/stdc++.h>
using namespace std;
int32_t main(void)
{
    freopen("input.txt", "r", stdin);
    clock_t start, end;
    start = clock();

    end = clock();
    printf("\nTime taken :  %lf\n", (((double)end - start) / CLOCKS_PER_SEC));
}